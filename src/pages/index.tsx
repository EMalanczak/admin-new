import { Content } from '../components/layout/content';
import { Layout } from '../components/layout/layout';

const Home = () => (
    <Layout>
        <Content label="Dashboard">Dashboard</Content>
    </Layout>
);

export default Home;
